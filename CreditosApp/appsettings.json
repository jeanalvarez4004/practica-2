{
  "ConnectionStrings": {
    "DefaultConnection": "DataSource=app.db;Cache=Shared"
  },
  "Redis": {
    // Local: vacio => cache en memoria. Render: Redis__ConnectionString con la URL de Redis Cloud.
    "ConnectionString": ""
  },
  "RabbitMq": {
    // Local: vacio => se advierte y no se encola; consumidor no inicia.
    // Render: variables RabbitMq__ConnectionString (amqps), RabbitMq__QueueName, RabbitMq__ConsumerEnabled.
    "ConnectionString": "",
    "QueueName": "solicitudes.notificaciones",
    "ConsumerEnabled": true
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
